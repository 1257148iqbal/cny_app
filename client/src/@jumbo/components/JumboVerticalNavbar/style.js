import { styled } from "@mui/material";

export const ArrowWrapper = styled('span')({
    position: "absolute",
    left: 5,
    display: "inline-flex",
    top: "50%",
    transform: 'translateY(-50%)'
});

